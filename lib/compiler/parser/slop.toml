# Native SLOP Parser
# Build with: uv run slop build (from this directory)

[project]
name = "slop-parser"
version = "0.1.0"
entry = "main.slop"

[build]
output = "slop-parser"
include = ["../common", "../../std", "../../std/strlib", "../../std/io", "../../std/os"]
type = "executable"
