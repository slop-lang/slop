;; Test: Function calls - should transpile multiple functions

(fn add ((a Int) (b Int))
  (@intent "Add two integers")
  (@spec ((Int Int) -> Int))
  (+ a b))

(fn main ()
  (@intent "Test function call")
  (@spec (() -> Int))
  (add 10 32))
