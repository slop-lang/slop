# Build configuration for the self-hosted SLOP compiler
#
# This enables `slop check` to resolve imports between compiler modules.

[build]
include = [
    "common",
    "parser",
    "transpiler",
    "checker",
    "../../lib/std/strlib",
    "../../lib/std/io"
]
