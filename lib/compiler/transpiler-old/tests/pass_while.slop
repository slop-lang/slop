;; Test: While loop - should transpile to C while loop

(fn main ()
  (@intent "Test while loop")
  (@spec (() -> Int))
  (let ((mut i 0)
        (mut sum 0))
    (while (< i 10)
      (set! sum (+ sum i))
      (set! i (+ i 1)))
    sum))
