(module main
  (import core (core-arena-new 1))
  (import strings (concat 2))

  (fn main ()
    (@intent "Entry point")
    (@spec (() -> Int))
    (let ((arena (core-arena-new 4096)))
      (println (concat "Hello, " "World!"))
      0)))
