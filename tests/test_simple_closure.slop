;; Simplest closure test
(module test-simple-closure
  (export main)

  ;; Direct let with lambda - no nesting
  (fn main ()
    (@intent "Main")
    (@spec (() -> Int))
    (let ((y 10)
          (f (fn ((x Int)) (+ x y))))
      (if (== (f 5) 15) 0 1)))
)
